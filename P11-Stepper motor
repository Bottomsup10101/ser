import RPi.GPIO as GPIO
from time import sleep
import sys

motor_channel = (29,31,33,35)

GPIO.setwarnings(False)
GPIO.setmode(GPIO.BOARD)
GPIO.setup(motor_channel, GPIO.OUT)

motor_direction = input('select motor direction a=anticlockwise, c=clockwise: ')

while True:
    try:
        if(motor_direction == 'a'):
            ...
        elif(motor_direction == 'c'):
            ...
    except KeyboardInterrupt:
        motor_direction = input(...)
        if(motor_direction == 'q'):
            print('motor stopped')
            sys.exit(0)















GPIO.output(motor_channel,(HIGH,LOW,LOW,HIGH))
sleep(0.02)

GPIO.output(motor_channel,(HIGH,HIGH,LOW,LOW))
sleep(0.02)

GPIO.output(motor_channel,(LOW,HIGH,HIGH,LOW))
sleep(0.02)

GPIO.output(motor_channel,(LOW,LOW,HIGH,HIGH))
sleep(0.02)  


